# external-ai-context-injector
A workflow pattern that routes requirement structuring to an external AI, then injects the result into Claude via file reference — cutting conversation-token cost by up to 85% over long sessions.
